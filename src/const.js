const POINTS_COUNT = 3;

const DateFormat = {
  POINT: 'h:mm',
  FORM: 'DD/MM/YY h:mm',
  DAY: 'DD MMM YY',
};

const SortType = {
  DEFAULT: 'default',
  PRICE: 'price',
  DATE: 'date',
};

export { POINTS_COUNT, DateFormat, SortType };
